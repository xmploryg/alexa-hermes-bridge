# Template only — do NOT commit real values.
# Create the real secret with:
#   kubectl -n alexa-bridge create secret generic bridge-secrets \
#     --from-literal=HERMES_API_KEY=<value-of-API_SERVER_KEY-from-hermes-.env>
apiVersion: v1
kind: Secret
metadata:
  name: bridge-secrets
  namespace: alexa-bridge
type: Opaque
stringData:
  HERMES_API_KEY: "REPLACE_ME"
